import "../../support/hooks"
// import { Company, User } from "app/models"

// describe(__filename, () => {

//   describe("company", async () => {
//     let user
//     let company

//     beforeEach(async () => {
//       company = await factory.create('company')
//       user = await factory.create('user', { company: company.id })
//     })

//     it('should populate company', async () => {
//       user = await User.queryOne({ id: user.id }).exec()

//       expect(user.company).to.eql(company.id)

//       await user.populateCompany()

//       expect(user.company.id).to.eql(company.id)
//     })
//   })

// })
